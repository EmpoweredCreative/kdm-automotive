# Get a free access key at https://web3forms.com — set destination email in their dashboard.
# Forge: Site → Environment → add VITE_WEB3FORMS_ACCESS_KEY=... so it exists when `npm run build` runs.
VITE_WEB3FORMS_ACCESS_KEY=
